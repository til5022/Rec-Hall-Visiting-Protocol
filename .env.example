# Copy this file to .env for local development (npm run dev).
# In production (Netlify), set these under Site configuration →
# Environment variables instead of committing a .env file.

VITE_AIRTABLE_TOKEN=
VITE_AIRTABLE_BASE_ID=
VITE_AIRTABLE_TABLE=Schedule
